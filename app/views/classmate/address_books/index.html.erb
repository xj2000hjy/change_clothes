<%= render :partial => 'partial/address_list', :collection => @addresses, :as => :addr %>
或
<% @addresses.each do |addr| %>
  <%= render :partial => 'partial/address_list', :locals => {:addr => addr}

<% end %>
<%= link_to '登录', address_books_login_path %>docs